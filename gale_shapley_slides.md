---
marp: true
paginate: true
---

# Gale-Shapley 稳定匹配算法
### 用校友交友 App 的真实数据讲一遍

---

## 背景：为什么不能只推荐"最像的人"

- 之前的推荐算法：给用户 A 算出所有候选人的 cosine similarity，推荐分数最高的那个
- 问题：**分数是对称的，但"最佳选择"不是对称的**
  - A 眼里 B 是第一名，不代表 B 眼里 A 也是第一名
  - B 可能是个"万人迷"，跟很多人都像，B 自己的真实第一选择是别人
- 结果：可能一直把 A 推给一个根本不会回头选 A 的人

---

## 核心概念：什么是"稳定"

一个配对方案是 **稳定** 的，指的是：

> 不存在两个没有配对在一起的人 X 和 Y，同时满足
> "X 更喜欢 Y 胜过自己现在的对象" **且** "Y 更喜欢 X 胜过自己现在的对象"

这种"互相更想在一起却没配对"的组合叫 **blocking pair**。

Gale-Shapley 算法保证：算出来的结果里，**一定不存在 blocking pair**。

---

## 算法机制：求婚-拒绝循环

前提：人群能分成两组（这里是"男生想认识女生" / "女生想认识男生"），每个人对另一组按喜欢程度排一份名单。

```
初始化：所有求婚方都是"单身"
while 单身池里还有人:
    P = 单身池里拿一个人出来
    如果 P 的候选列表已经求完了：P 保持单身，跳过
    Q = P 列表里排名最高、还没求过婚的人
    P 向 Q 求婚（这个动作只会发生一次）

    如果 Q 现在没有对象：Q 接受 P
    否则比较 Q 现在的对象 R：
        Q 更喜欢 P → 接受 P，甩了 R，R 回到单身池
        Q 更喜欢 R → 拒绝 P，P 回到单身池，下次求下一个
```

---

## 关键性质：单向的此消彼长

- **求婚方的处境只会越来越差**：每次换目标都是列表里排名更低的人
- **接收方的处境只会越来越好**：只会换到自己更喜欢的人

这个单向性，是"一定会停下来"和"停下来时是稳定的"两个定理的根本原因。

---

## 定理一：一定会终止

- 每个求婚方最多向自己列表里的每个人求婚 **一次**（求过的不会再求第二次）
- 总求婚次数 ≤ 求婚方人数 × 接收方人数（一个有限的上界）
- 每转一圈循环都消耗一次求婚配额
- → 循环必然在有限步内结束，不会死循环

---

## 定理二：终止时一定稳定（反证法）

假设结果里存在 blocking pair (X, Y)：

1. X 更喜欢 Y → X 一定向 Y 求过婚（按列表从高到低顺序求婚，不会跳过喜欢的人）
2. Y 要么接受了 X（但结果不是这样，矛盾），要么拒绝了 X
3. Y 只会拒绝比"当前对象"更差的人，而且 Y 的对象只会越换越好
4. 所以 Y 最终的对象一定不比拒绝 X 那一刻的对象差 → 比 X 更好

这跟"Y 更喜欢 X 胜过现在的对象"矛盾 → blocking pair 不存在

---

## 真实例子：清华大学的求婚过程

同校 + 双向性别偏好过滤后：

| 男生（求婚方） | 女生（接收方） |
|---|---|
| 王志远、张伟、陈明、孙浩 | 李思思、刘晓萱、赵雅婷 |

（周静怡选了"都可以"，不在这个二分图里，不参与）

4 个男生，3 个女生 —— 注定有一个人这轮配不到。

---

## 每个人的偏好列表（用问卷+简介算出来的融合分排序）

**赵雅婷的偏好顺序**：陈明 > 张伟 > 王志远 > 孙浩

**张伟的偏好顺序**：刘晓萱 > 赵雅婷 > 李思思

（其他人的列表同理，都是用 cosine similarity 算出来、按分数排序的）

---

## 求婚过程 Step 1-3：第一轮，各求各的第一志愿

```
1: 孙浩 向 李思思 求婚(第1志愿) -> 她还没对象，接受
2: 陈明 向 赵雅婷 求婚(第1志愿) -> 她还没对象，接受
3: 张伟 向 刘晓萱 求婚(第1志愿) -> 她还没对象，接受
```

三个人当场配对成功（暂时），孙浩、陈明、张伟先松一口气。

---

## 求婚过程 Step 4：迟到的王志远搅局

```
4: 王志远 向 李思思 求婚(第1志愿) -> 她更喜欢新来的，甩了 孙浩
```

李思思发现王志远比孙浩更合她心意 → **换人**，孙浩被甩回单身池。

孙浩：😢 但没关系，规则是"下次求列表里第2志愿"，绝不会回头再求李思思。

---

## 求婚过程 Step 5-7：孙浩一路碰壁

```
5: 孙浩 向 赵雅婷 求婚(第2志愿) -> 她更喜欢现在的对象(陈明)，拒绝
6: 孙浩 向 刘晓萱 求婚(第3志愿) -> 她更喜欢现在的对象(张伟)，拒绝
7: 孙浩 列表求完了，还是单身
```

孙浩的列表（3个候选人）全部求完，无人接受 → 循环结束时孙浩落单。

---

## 最终结果

```
王志远 ↔ 李思思
张伟   ↔ 刘晓萱
陈明   ↔ 赵雅婷
孙浩   —— 落单（4男3女，人数不对等，必然有人配不到）
```

用 blocking pair 检测脚本验证过：**这个结果数学上确认稳定**，
没有任何一对没配对的人会同时都更想跟对方在一起。

---

## 这个算法的适用范围

- 经典证明要求人群能干净地分成"两组"（这里靠"男生/女生"的性别互选实现）
- 一旦有人是双性恋/同性向往，两个人可能同时属于"求婚方"，二分结构被打破
- 这种更通用的情况叫 **稳定室友问题**，需要更复杂的 Irving 算法，且**不保证一定有解**

---

## 后续演进：新版 App 换成了贪心最大权重匹配

新的 Common Ground 产品是"价值观匹配"，没有性别/性向的二分结构
（谁都可能跟谁匹配），Gale-Shapley 的前提不成立了。

所以换成了更通用的做法：

1. 算出所有人两两之间的加权相似度
2. 按分数从高到低排序
3. 依次锁定分数最高、且两人都还没被配对的一对
4. 重复直到没人可配

同样保证"一个人最多配一次"，但不需要二分图的前提。

---

## 总结

- Gale-Shapley 解决的问题：让"每个人的推荐"也考虑"对方是否也想要你"
- 核心机制：求婚-拒绝循环，单向此消彼长
- 两大定理：**一定终止** + **终止时稳定**（无 blocking pair）
- 局限：只适用于能分两组的二分图场景
- 我们的校友 App 验证了它在真实数据上确实稳定；后来的新产品换成了适用范围更广的贪心匹配

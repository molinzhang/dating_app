DATABASE_URL=postgresql://postgres.xxxxxxxxxxxx:[YOUR-PASSWORD]@aws-0-us-east-2.pooler.supabase.com:5432/postgres

# Secret for POST /api/admin/run-matching, the only thing that runs pairing.
# Leave unset to disable that endpoint entirely (it returns 503).
ADMIN_TOKEN=change-me-to-a-long-random-string
